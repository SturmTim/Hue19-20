<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ToDoApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/toolBar</item>
        <item name="colorPrimaryVariant">@color/toolBar</item>
        <item name="colorOnPrimary">@color/defaultColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/checkBox</item>
        <item name="colorSecondaryVariant">@color/defaultColor</item>
        <item name="colorOnSecondary">@color/defaultColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/textBackground</item>
    </style>
</resources>